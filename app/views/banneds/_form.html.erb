<%= form_with(model: banned, local: true) do |form| %>
  <% if banned.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banned.errors.count, "error") %> prohibited this banned from being saved:</h2>

      <ul>
      <% banned.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :banned_email %>
  </div>

  <div class="field">
    <%= form.label :msg %>
    <%= form.text_field :msg, id: :banned_msg %>
  </div>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_field :reason, id: :banned_reason %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
